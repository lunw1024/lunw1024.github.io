{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "deploy": "pnpm generate && cd .output/public && touch .nojekyll && echo alanwu.xyz >> CNAME && git init -b gh-pages && git add . && git commit -m 'deploy' && git remote add origin https://github.com/lunw1024/lunw1024.github.io.git && git push -f origin gh-pages:gh-pages"
  },
  "devDependencies": {
    "@iconify/vue": "^4.1.2",
    "@nuxtjs/tailwindcss": "^6.12.1",
    "@vueuse/core": "^11.1.0",
    "@vueuse/nuxt": "^11.1.0",
    "nuxt": "^3.13.2"
  },
  "dependencies": {
    "daisyui": "^4.12.10",
    "p5": "^1.11.0"
  }
}
